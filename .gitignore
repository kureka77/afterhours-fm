# Secrets — never commit
.env

# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
coverage/
# Cobertura report generated by the SonarQube workflow
coverage.xml

# Local database
app.db

# Node
node_modules/
package-lock.json

# Playwright MCP scratch output — page snapshots and console logs from browser
# verification. Not ignored before, and they capture whatever was on screen,
# which for this app includes live track data.
.playwright-mcp/

# Editor / OS / tooling
.DS_Store
.claude/
.idea/
.vscode/
